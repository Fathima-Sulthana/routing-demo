import { Metadata } from "next"

export const metadata: Metadata = {
    title: {
        absolute: "New dashboard"
    }
}

export default function ComplexDashboardPage(){
    return <h1>Complex Dashboard</h1>
}